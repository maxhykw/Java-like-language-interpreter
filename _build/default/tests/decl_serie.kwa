var int x, y, z;
var bool a, b;

main {
  x = 1;
  y = 2;
  z = 3;
  a = true;
  b = false;

  print(x); // 1
  print(y); // 2
  print(z); // 3
  if (a) { print(1); } else { print(0); } // 1
  if (b) { print(1); } else { print(0); } // 0
}
